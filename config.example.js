// انسخ هذا الملف باسم config.js واملأ القيمتين من مشروع Supabase
// (Project Settings ▸ API). استخدم مفتاح anon public فقط — لا تضع service_role إطلاقاً.
//
// إذا تركت القيم فارغة، يعمل التطبيق محلياً بالتخزين المحلي (localStorage) كما هو.
window.FA_CONFIG = {
  SUPABASE_URL: "",       // مثال: https://xxxxxxxx.supabase.co
  SUPABASE_ANON_KEY: ""   // مفتاح anon public الطويل
};
